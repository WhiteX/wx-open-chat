{
  "name": "wx-open-chat",
  "version": "0.2.0",
  "description": "OpenClaw agent sidebar for Obsidian",
  "main": "main.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "echo 'lint skipped (fresh baseline)'",
    "test": "echo 'no tests yet'"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "builtin-modules": "5.0.0",
    "esbuild": "0.27.3",
    "obsidian": "latest",
    "tslib": "2.8.1",
    "typescript": "5.9.3"
  }
}
